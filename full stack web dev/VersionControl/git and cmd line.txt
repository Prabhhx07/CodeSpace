code (filename)=>to open file;
git init=> to intialise .git file in repository;
ls=>to list files;
ls-a => to list all files including hidden;
git status => to track files in dir;
git add file_name => adds change in working dir to staging area;
git add . =>adds everyfile;
git commit -m "" =>commit changes with a msg;
git log => shows all the commits,exit using 'q';
git diff file_name => shows diff in file;
git checkout file_name =>revert backs to last version created in locacl repository;
git branch branch_name =>adds a branch;
git merge branch_name => merges branch;